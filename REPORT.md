# 🌅 早间验收报告（2026-06-12 凌晨施工，一夜完成）

## 现在就能用

| 入口 | 地址 / 说明 |
|---|---|
| **Web 控制台** | http://76.13.182.175:8488 （账号 `admin` / `trade@2026`，**登录后请改密码**） |
| **Telegram** | @trade_flyyy_bot — RR≥5 主信号卡片+确认按钮；发 `/status` 查状态；每天 08:00 自动日报 |
| **域名 HTTPS** | 等你在 DNS 面板加一条 A 记录：`trade` → `76.13.182.175`，几分钟后 https://trade.overall.it.com 自动生效 |

## 系统状态（截至 05:30）

- VPS（吉隆坡）systemd 服务 `trade` 运行中，开机自启、崩溃自动重启
- 监控 **55 个**高流动性 USDT 永续合约（24h 成交额≥5000 万 U，列表每小时自动刷新），5m + 15m 双级别，K线收盘到分析完成 **<2 秒**
- 内存稳定 ~93MB，整夜 **0 错误、0 重连**，K线库 5.8 万行
- **整夜 0 信号** —— 已专项排查：用同一时间窗对全部币种做回放交叉验证，结果一致，确认是凌晨行情清淡 + 规则严格所致，**不是 bug**

## 一夜建成的东西

1. **缠论信号引擎**：K线包含处理 → 顶/底分型 → 量能放大(≥1.5x均量) → 跌破收回 → 1h 趋势过滤 → Volume Profile 密集成交区止盈 → 前低止损 → RR 双轨（≥5 主信号推TG / ≥2.5 入库统计）
2. **7 因子打分系统**（你半夜提的需求）：RSI背离+2、RSI超卖/超买、主动买盘占比、影线拒绝、资金费率极值、5m/15m共振、BTC共振，各+1；ATR 止损合理性硬过滤。**每个信号的因子明细入库**，积累数据后可统计各因子对胜率的真实贡献
3. **Paper 模拟盘**：信号自动虚拟成交，TP/SL 自动结算，胜率/期望R/权益曲线双轨对比——这是验证"模式是否赚钱"的核心
4. **TG 确认下单链路**：卡片 → 确认 → 二次确认 → （live 模式）币安市价入场+自动挂TP/SL。实盘代码就绪，等你的 API 密钥联调
5. **Web 控制台**：登录鉴权、实时信号流、持仓统计、21 项参数在线热调、K线图带信号标注
6. **工程化**：18 个单元测试、git 全程提交、一键部署脚本(deploy/deploy.ps1)、SSH密钥、每小时数据自维护、健康报告工具

## 需要你做的 3 件事

1. **加 DNS A 记录**：`trade` → `76.13.182.175`（启用域名+HTTPS）
2. **币安 API 密钥**（想实盘时）：只开"合约交易"权限、**禁止提现**、IP 白名单填 `76.13.182.175`，密钥填到 VPS 的 `/opt/trade/.env` 后 `systemctl restart trade`
3. **改 Web 密码**

## 交易专家建议（基于昨夜数据）

- **RR≥5 门槛极严**：5 天历史回放只出 1 个主信号。建议先观察双轨 paper 数据 3-7 天再定夺
- **想加快验证样本积累**，可在 Web 配置页调（都能热生效）：`量能倍数 1.5→1.3`、`次级RR门槛 2.5→2.0`、或临时关闭趋势过滤。样本够了再收紧
- 当前 `factors.min_score=0`（因子只记录不拦截），等积累 50+ 笔后做因子归因分析，数据说话再决定哪些因子设为硬门槛
- 白天行情活跃后信号频率会明显上升，留意 TG
