# Live 200 USDT Notional Sizing Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Set every future live automatic entry to 100 USDT fixed margin at 2x leverage, approximately 200 USDT notional.

**Architecture:** Use the authenticated localhost settings API to update the running process and SQLite overrides together without deploying code. Freeze new entries, snapshot current positions and protection records, change the full approved setting set, verify unchanged positions, restore automation, and independently re-read the final state.

**Tech Stack:** PowerShell, SSH, Python 3, FastAPI localhost API, SQLite, Binance Futures account API, systemd.

---

### Task 1: Guarded Online Update

**Files:**
- Read: `docs/superpowers/specs/2026-08-25-live-200u-sizing-design.md`
- Modify externally: `/opt/trade/data/trade.db` through the running localhost API
- No application code modified

- [x] Verify remote revision `2a96f59a40122dba9c85e3266142d9fe14fba95a`, active service, and HTTP 200.
- [x] Read and retain the current `live.fixed_margin_u` value for rollback.
- [x] Set `live.auto_trade=false` before reading positions.
- [x] Snapshot all non-zero Binance positions and their database stop-loss/take-profit order records.
- [x] POST the complete approved settings: live mode, auto false, equity 1479.41, leverage 2, fixed margin 100, percentage/notional fallbacks zero, maximum positions 1, loss breaker 2%, both directions.
- [x] Read back every setting and require exact equality.
- [x] Re-read positions and protection records and require no change from the snapshot.
- [x] Set `live.auto_trade=true` only after all checks pass.
- [ ] On any failure, restore the observed prior fixed margin, leave auto trading false, and do not touch positions or orders.

### Task 2: Independent Verification

**Files:**
- No repository files modified

- [x] Reconnect in a separate process and verify `live.fixed_margin_u=100`, leverage 2, `live.auto_trade=true`, maximum positions 1, and loss breaker 2%.
- [x] Verify existing Binance positions and recorded protective orders remain unchanged.
- [x] Verify `trade.service` is active, localhost HTTP is 200, engine mode is live, WebSocket is connected, and recent service logs contain no new errors from the settings update.

### Task 3: Audit Handoff

**Files:**
- Create: `docs/superpowers/plans/2026-08-25-live-200u-sizing.md`
- Modify: `docs/journal/daily/2026-08-25.md`

- [x] Mark only executed plan steps as complete.
- [x] Record the previous and final settings, position/protection snapshot, service health, verification commands, risks, and VPS live scope.
- [x] Run `git diff --check` and inspect `git status --short`; do not include `.env` or unrelated existing changes.
- [x] Commit only this plan and the 2026-08-25 journal with message `ops: set live sizing to 200u notional`.
