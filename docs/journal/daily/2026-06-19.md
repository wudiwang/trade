# 2026-06-19 Daily Log

## Key Ideas

- Build a personal trading research platform, not just a signal script.
- Use one repository with separate local research and VPS live application entry points.
- Local backtest system should become a Strategy Lab with strategy details, metrics, chart review, timeframe switching, and case saving.
- Add AI Watch Agent to monitor selected symbols on 1m/5m and alert on symbolic K lines.
- Add macro research for BTC regime, YouTube analyst summaries, and broader liquidity context.

## Key Decisions

- Local is research lab; VPS is execution room.
- Strategies must be locally backtested and chart-reviewed before going online.
- Multi-agent work must have role boundaries and handoff files.
- Claude Code can execute planned tasks, but Codex must review diff, tests, and safety before accepting.
- Claude Code loop may continue through non-blocked tasks overnight, but every completed task must append a checkpoint with files changed, verification evidence, risks, and next step.
- Codex heartbeat inspection is active hourly for this thread to review Claude Code checkpoints without requiring the user to wake up and trigger review manually.

## Todo Added

- Create knowledge base and Claude Code loop instruction.
- Upgrade deployment to version-controlled flow.
- Upgrade local backtest viewer into Strategy Lab.
- Add data refresh scheduling plan and freshness visibility.
- Add Agent Workbench and Watch Agent design.
- Keep deployment and live-trading changes behind explicit review gates during autonomous work.

---

## Checkpoint (Claude Code loop)

Task completed: Task 1 — Knowledge Base Foundation (verify files exist)
Files changed: docs/superpowers/plans/2026-06-19-trade-platform-phase1.md (checked Task1 box)
Verification commands: `for f in AGENTS.md docs/knowledge/*.md docs/journal/todos.md .claude/commands/trade-loop.md; do test -f $f; done`
Results: all 13 referenced files present (AGENTS.md, all docs/knowledge/*.md, todos, trade-loop.md).
Risks: none. Local docs only.
Next recommended step: Task 2 deployment scaffolding.

Task completed: Task 2 — Versioned Deployment (scaffold; VPS verify blocked)
Files changed: deploy/verify_remote.ps1 (new, read-only verifier), deploy/remote_deploy.sh (new, git-deploy scaffold, not enabled), deploy/deploy.ps1 (REVISION stamping, additive)
Verification commands: `powershell -File deploy/verify_remote.ps1`
Results: read-only verify PASS — local HEAD 7998795 / remote REVISION none(legacy tar) / service active / web 200 / strategy func present / HEALTHY. No deploy executed.
Risks: deploy.ps1 here-string switched '@..'@ -> "@..@" to interpolate $REV; body has no other $ vars so safe, but NOT run on VPS yet (unverified end-to-end). .env/data/logs preserved because gitignored.
Next recommended step: Task 3 — local data freshness display (cache_status endpoint). Branch note: working tree reset to 7998795 by concurrent codex session; 638 uncommitted files; not committing per Codex-approval gate.

Task completed: Task 3 — Local Data Freshness Display
Files changed: scripts/bt_registry.py (cache_status()), scripts/bt_viewer.py (/api/cache_status endpoint + #fresh top strip + loadFresh JS)
Verification commands: GET http://127.0.0.1:8530/api/cache_status ; GET /api/signals
Results: endpoint returns klines{5m,15m,1h last_open+age_min} and signals{<strat> generated+count}. Live: 5m age 69m / 15m 89m / 1h 134m. Viewer now serves all 5 strategies (32967 signals: pullback 22899, smallbig 4796, macro_pullback 3796, reversal 1182, deepbase 294).
Risks: kline freshness uses BTCUSDT as representative symbol; fine since refresh updates all symbols together. pullback emits 22.9k signals (heavy but viewer reads precomputed JSON so still fast).
Next recommended step: Task 5 timeframe switching (kline endpoint already accepts tf param).

Task completed: Task 5 — Chart Timeframe Switching
Files changed: scripts/bt_viewer.py (renderSig/switchTf/snap, #tfsw selector)
Verification commands: GET /api/klines?...&tf=5m|15m|1h for a sample signal
Results: 5m=20 / 15m=20 / 1h=13 bars returned for same center; markers snap via bisect; entry/sl/tp lines redrawn each render. Viewer live with all 5 strategies (32967 signals).
Risks: 1h window narrower (span bars are per-tf so 1h covers more wall-clock); acceptable. macro_pullback/reversal default to 5m base via defTf().
Next recommended step: Task 4 strategy detail metadata panel.

Task completed: Task 4 — Strategy Detail metadata + panel
Files changed: scripts/bt_viewer.py (DETAIL overlay, /api/meta detail, detail panel render)
Verification commands: GET /api/meta -> detail
Results: detail for all 5 strategies (smallbig/pullback/deepbase/reversal/macro_pullback) with desc/idea/updated/code/doc; panel renders 简介/原始想法/更新/代码/文档 + 当前逻辑.
Risks: CONCURRENCY — bt_registry.py is being actively rewritten by codex session (now contains inlined macro_pullback detection). Deliberately kept Task 4 metadata in bt_viewer.py to avoid clobber. Task 7 (metrics) will need bt_registry.py/bt_scan.py edits → high conflict risk, recommend codex owns it or coordinate.
Next recommended step: Task 9 Agent Workbench table (conflict-free, viewer + reads docs/agents).

Task completed: Task 9 — Agent Workbench Table
Files changed: scripts/bt_viewer.py (_agents_data, /api/agents, /agents page, header link)
Verification commands: GET /api/agents ; GET /agents
Results: 3 agents listed (strategy-orchestrator, strategy-researcher, researcher:smallbig w/ handoff docs/agents/notes_smallbig.md); /agents page HTTP 200. No external services.
Risks: none; read-only local scan. "next task" is the tail of the note rather than a structured field (acceptable for v1).
Next recommended step: Task 10 watch model stub + tests (clean new files, TDD).

Task completed: Task 10 — Watch Agent Data Model Stub
Files changed: scripts/watch_model.py (new), tests/test_watch_model.py (new)
Verification commands: .venv/Scripts/python -m pytest tests/test_watch_model.py -q
Results: 10 passed in 0.04s. Covers long/short trigger, close-based invalidation, invalidation-precedence-over-trigger, expiry, watching, sticky terminal/triggered, no input mutation.
Risks: none; pure functions, no live wiring. Trigger = price touches trigger_price; invalidation = close beyond invalidate_price (chosen as conservative).
Next recommended step: Task 11 macro scaffold (new file + test) then Task 6 pattern-case save.

Task completed: Task 11 — Macro Research Scaffold
Files changed: scripts/macro_ingest.py (new), tests/test_macro_ingest.py (new), artifacts/macro/.gitkeep (new), docs/journal/todos.md (blocked detail)
Verification commands: pytest tests/test_macro_ingest.py -q ; macro_ingest.py /tmp/macro_sample.txt
Results: 5 passed. CLI wrote artifacts/macro/macro_*.json (bias=long, levels [60000,65000]); --youtube prints BLOCKED exit 2.
Risks: keyword-based bias is naive (v1); fine as scaffold. No network.
Next recommended step: Task 6 pattern-case save (viewer) OR Task 12 review gate.

Task completed: Task 6 — Save Classic Pattern Case
Files changed: scripts/bt_viewer.py (POST /api/save_case + ⭐保存案例 button), pattern_cases/.gitkeep, artifacts/pattern_cases/.gitkeep
Verification commands: POST /api/save_case {id, label} for a live signal
Results: wrote pattern_cases/macro_pullback/<sym>_<ts>.json with symbol/strategy/tf/timestamp/entry/sl/tp/result/markers/label/direction/saved_at; test artifact then removed.
Risks: none; local file write under pattern_cases/.

Task completed: Task 12 — Review and Safety Gate
Files changed: docs/journal/* , docs/superpowers/plans/2026-06-19-trade-platform-phase1.md (checkboxes)
Verification commands: pytest (15 passed) ; git status .env ; grep auto_trade config.yaml ; git status --porcelain
Results: tests 15/15 pass. .env untouched. auto_trade=false. No edits to live files (trader.py/signals.py). Changed = local research/docs/deploy-scaffold/tests only.

=== Loop session summary (for Codex) ===
Done this loop: Task 1 (verify docs), Task 2 (deploy scaffold: verify_remote.ps1 read-only PASS, deploy.ps1 REVISION stamp, remote_deploy.sh; VPS deploy-verify BLOCKED on approval), Task 3 (cache_status freshness), Task 4 (strategy detail panel), Task 5 (TF switching), Task 6 (save pattern case), Task 9 (agent workbench /agents), Task 10 (watch_model + 10 tests), Task 11 (macro_ingest + 5 tests; YouTube BLOCKED).
Deferred: Task 7 (metrics) — needs scripts/bt_registry.py + bt_scan.py which the CONCURRENT codex session is actively rewriting; deferred to avoid clobber. Task 8 (composer stub) — not started (viewer-only, next).
Not committed: per Codex-approval gate + concurrent branch churn (working tree was reset to 7998795 by codex session; 600+ uncommitted incl. regenerated .btcache). All work is in the working tree for `git diff` review.
Viewer running locally on :8530 (all 5 strategies, freshness, TF switch, detail, agent workbench, save-case).
