# 2026-08-25

Task completed: Raise future live automatic entries to approximately 200 USDT notional.

Files changed:
- `docs/superpowers/plans/2026-08-25-live-200u-sizing.md`
- `docs/journal/daily/2026-08-25.md`

Commands run:
- Guarded remote Python update through the authenticated localhost `/api/settings` endpoint.
- Independent remote Python read-back of effective settings, Binance positions, wallet balance, service state, HTTP state, events, and recent journal errors.
- `journalctl -u trade --since @1787596581 --no-pager`, `systemctl is-active trade`, and localhost HTTP verification.

Results:
- Deployed revision remained `2a96f59a40122dba9c85e3266142d9fe14fba95a`; no code was deployed.
- Previous fixed margin was `10 USDT`; final fixed margin is `100 USDT`.
- Leverage is `2x`, so future nominal position size is approximately `200 USDT` before quantity-step rounding.
- `live.auto_trade=true`, `live.max_positions=1`, `live.max_loss_pct=2`, and trade direction remains both.
- Percentage and fixed-notional sizing fallbacks remain zero.
- No Binance position was open during the update, so no position or protective order was changed.
- Independent verification showed wallet balance `1504.25411826 USDT`, service `active`, HTTP `200`, live mode, and one connected WebSocket.
- The ETH minimum-notional error immediately before the update belonged to the former 20 USDT nominal sizing. Journal output since the settings update contained no entries or new errors.

Risks:
- Real Binance Futures orders remain enabled and future entries may reserve approximately 100 USDT margin each.
- Actual notional and profit vary with price, exchange quantity-step rounding, fees, slippage, and funding.
- The 2% balance breaker prevents new entries but does not close an existing position.

Scope: VPS live trading.

Next recommended step: Verify the first post-change fill is approximately 200 USDT notional and has both stop-loss and take-profit protection orders.
